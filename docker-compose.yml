version: '3'
services:
  frontend:
    build: ./frontend
    volumes:
      - ./frontend/src:/app/frontend/src
    ports:
      - 8000:8000
  backend:
    build: ./backend
    volumes: 
      - ./backend:/app/backend
    ports:
      - 8080:8080
    command: go run main.go